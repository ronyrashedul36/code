#include<bits/stdc++.h>
#define ll long long
using namespace std;
int main()
{
    ll n,k,ma,cnt,f,t;
    ma=cnt=0;
    /*cin>>n>>k;
    while(n--)
    {
        cin>>f>>t;
        if(t>k)
            cnt=f-(t-k);
        else
            cnt=f;
        if(ma<0 && cnt<0)
            ma=min(ma,cnt);
        else if(ma==0 && cnt<0)
            ma=cnt;
        else
            ma=max(ma,cnt);
    }
    cout << ma << endl;*/
    cout << min(-2,-3);
    return 0;
}
